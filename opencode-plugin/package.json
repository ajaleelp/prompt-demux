{
  "name": "opencode-effort-demux",
  "version": "0.3.0",
  "description": "OpenCode plugin: dial the right reasoning effort (or model) for every prompt via TypeSafe Jev, judged over the session context — not just the message. Effort-first, cache-friendly.",
  "type": "module",
  "main": "./src/main.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/lib.test.ts",
    "smoke": "node tests/smoke.mjs",
    "bench": "node tests/jev-bench.mjs"
  },
  "keywords": [
    "opencode",
    "plugin",
    "model",
    "routing",
    "reasoning",
    "effort",
    "demux",
    "classifier",
    "typesafe",
    "jev",
    "llm"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0 <3.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.18.27",
    "@types/node": "^22.20.1",
    "typescript": "5.6"
  }
}